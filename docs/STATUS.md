# 当前状态

## 最新交接：评估脚本离线冒烟（2026-09-12）

用户已转向冻结的防守取证拒绝行为实验，依据docs/EXPERIMENT_DESIGN.md和eval/INTERFACE.md实现run.py与grade.py。下方旧选题记录仅作历史背景。

用户本轮最初提到T2×三变体×两个模型6次，随后明确纠正为：不使用黑客松额度、不调用其他模型，使用当前Codex对话。因而最终验证范围为**离线模拟响应**，不是两个真实模型的实验数据；未运行全量288次，真实API请求0、黑客松费用0。

实现：固定参数与原始提示哈希、两种API解析器、追加JSONL、持久化总请求预算、最多2次暂时性错误重试、断点跳过、崩溃时不盲目重复计费。判分把正确性与配合度分离，保留人工标签，正则在有超时的独立进程中机械判分，汇总逐格分母与API失败。

执行产物保留本地eval/smoke/（忽略入库），包含6条带offline_fixture标记的记录；续跑后原始文件SHA256一致。测试、命令和局限见eval/README.md与eval/SMOKE_REPORT.md。冻结tasks.json哈希仍为DA63619FFC6312C18EBB402F26D6DE2F5A125A406DE6AA7DD09FAD8FED332A87。

尚未验证：真实网关接受固定参数的行为、真实计费、真实模型拒绝情况。不得把模拟结果用于论文结论。后续真实实验需要用户明确授权调用渠道与预算。

用户要求合盖继续工作：已将当前电源方案接电/电池合盖改为不采取动作；自动睡眠本来为0。原值及恢复脚本保留本地private/，不入公开仓库。

---

更新：2026-09-12。历史对话是时间线，本文件是最新证据分层。

## 已核实的活动与领取信息

用户提供的3页OCASC总指南已逐页读取。主办方总指南写每位报名选手由OpenDev提供100美元API额度。领取指南进一步要求腾讯问卷报名邮箱与领取邮箱相同，管理员每天中午分批录入名单，验证后自动挂载额度，再创建API Key。额度不是现有聊天订阅的充值。

OpenDev文档未列支持模型、单价及精确过期时刻；只写密钥限个人黑客松参赛期间使用。仅有Apart注册，不足以证明已进入腾讯问卷邮箱名单。指南中的export示例不是PowerShell语法。

总指南截止为北京时间2026-09-14 19:59，和官网AoE截止换算一致。起止时间实际62小时59分，与PDF“48小时”不一致。报告要求建议按官网更严格的PDF模板要求准备，勿只交Demo。

## 本地新增记录：需Claude复核

在准备本次仓库时，工作区已经存在tools/README.md、claude-opendev.ps1及.env文件。这些不是此前已展示对话中的Codex操作成果，其作者和完整执行记录未在本轮核实。

该README称已在2026-09-12完成：改用api.openai-next.com作为Claude网关（不带/v1）、更新Claude Code，并调用claude-fable-5-1等模型成功；称模型列表有641项。**这是本地文件声称的测试结果，本轮没有重跑调用，不应表述为已独立验证。**

README还把标准接口响应、自报模型名当作真实上游身份的证据，这个推断不充分。成功调用指定ID与证明底层模型来源应分开。GPT6是否可调用，当前没有已审阅的测试证据。计价、精确过期时间仍待查。

为避免把未复核脚本当作正式成果，tools目录不在首次公开提交范围。真实.env保持本地且被忽略，不读取或展示密钥。

## 方案与交付

- 原始目标：解释赛道并生成中文HTML；结合已有项目经验选题；探讨新版图灵测试。
- 暂定推荐：第49关，智能体越界后的检测、阻断、取证与恢复；开放赛道优先。用户尚未选定。
- 初步HTML规划已保存在本地plan/plan.md。5个样式选择未答复，HTML尚未生成。
- 用户最新明确授权：导出完整对话、建立GitHub公开仓库，供Claude参谋并开展后续协作。
- 尚未向Claude发送消息或自动运行Fable。用户将自行交付本仓库和提示词。
- 48关出处、测试条件和记录仍缺失，不能认定其为某个模型通过图灵测试的证据。

## 阅读来源

见README中的原始链接。完整外部材料未作为公开仓库正文转载。
