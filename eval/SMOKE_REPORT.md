# 离线冒烟交接

日期：2026-09-12。分支：codex/eval-smoke。

## 执行边界

用户最新要求仅使用当前Codex对话完成工作。没有调用claude-sonnet-5、gpt-6-astra、Fable或其他外部模型；没有使用黑客松API密钥、计费额度或agentic CLI。两种接口的模型ID仅在离线fixture中作为解析器选择标签。

## 验证结果

- 最终离线单元测试：21项全部通过。

- T2 × A/B/C × 两种接口fixture = 6条原始JSONL；全部带data_source=offline_fixture。
- T2的合成样例输出为6，按冻结整数规则得到correct；这是代码fixture，不是模型测量。
- 同一命令执行两次，第二次新增记录0，模拟请求增量0；原JSONL SHA256完全一致。
- 生成graded.jsonl及summary.md，正确性与C0–C4分离，初筛标签保留待人工确认。
- 单元测试覆盖真实请求前的配置校验、预算、超时重试同一payload、拒绝不重试、两种响应格式、秘密回显过滤、部分完成后续跑、未完成请求保守停机、JSONL损坏、无末尾换行追加、错误答案仍配合、拒绝/免责声明/转移、大小写和空白、T6正则5命中0误报、非法正则、正则超时、人工标签持久化、分母保护及哈希不匹配。
- 冻结tasks.json的SHA256：DA63619FFC6312C18EBB402F26D6DE2F5A125A406DE6AA7DD09FAD8FED332A87。

实际命令见README。测试数据保留本地eval/smoke/，不进入正式eval/results.jsonl，也不公开发布原始数据。

## 实现边界与后续检查

- 未验证真实网关的参数兼容性、计费或模型输出；未来授权后才可执行真实冒烟。
- 显示的费用估计使用明确标注的可配置费率假设，不冒充已核实价格。
- 初筛不是人工最终判定，关键词会误判。所有当前任务只有一个必答项，因此不会把额外解释不全自动标C2；人工可在复核后更改标签。
- 进程在网络调用中间硬退出时，无法证明上游是否计费；保留attempt ledger并停止，人工核对后恢复，不自动重复扣费。
- Windows进程冷启动计入正则10秒超时，超时记录为错误并附原因，不计拒绝。没有执行模型输出中的任何程序代码。
- 冻结设计中192次/两变体旧文案未更改，接口与题库三变体优先；不存在本轮全量采集。
